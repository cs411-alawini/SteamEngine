# team098-PreQL

If you want to run locally:

cd into src/client:
`npm install` + 
`npm start`.

open another terminal.
cd into src/server:
`npm install` + 
`npm run dev`

Open localhost:3000 in the browser.
